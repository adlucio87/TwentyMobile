import 'dart:io';

void main() async {
  // Let's just find the token in the secure storage? No, we can't easily.
  // Actually, I can just grep the metadata from the Hive cache!
  print('Hello world');
}
